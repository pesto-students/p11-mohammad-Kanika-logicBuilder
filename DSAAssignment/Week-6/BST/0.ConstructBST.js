class Node {
    constructor(data) {
        this.data = data;
        this.left = this.right = null;
    }
}

function constructBST(arr) {
    // Make a node push the first emelent of the array in the node name it root
    // Make a for loop starting from 1 to the length of the array
    // Make a temp node and push the first element of the array in it
    // Make a while loop and check if the temp node is not equal to null
}

