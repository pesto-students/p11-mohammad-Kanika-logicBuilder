function drawTriangle(triangleSize) {

    for(let outerLoop=1;outerLoop<=triangleSize;outerLoop++){
        let eachLine=""
        for ( let innerLoop=0;innerLoop<outerLoop;innerLoop++){
            eachLine=eachLine+"*"
           
        }
        console.log(eachLine);
    }

    // Your solution goes here
    
 }
